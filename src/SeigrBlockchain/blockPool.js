let blockPool = [];

const createBlockPool = () => {
  blockPool = [];
};

const getBlockPool = () => {
  return blockPool;
};

const saveBlockPool = () => {
  return blockPool;
};

const loadBlockPool = () => {
  return blockPool;
};

module.exports = { createBlockPool, getBlockPool, saveBlockPool, loadBlockPool };
