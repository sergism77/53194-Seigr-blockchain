'use strict';

const createMerkleLeaf = (transaction) => {
    //create the merkle leaf
    let merkleLeaf = {};
    //create the merkle leaf from the transaction
    merkleLeaf = transaction;
    //return the merkle leaf
    return merkleLeaf;
}

module.exports = createMerkleLeaf;