'use strict';

const createMerkleNode = (transaction) => {
    //create the merkle node
    let merkleNode = {};
    //create the merkle node from the transaction
    merkleNode = transaction;
    //return the merkle node
    return merkleNode;
}

module.exports = createMerkleNode;